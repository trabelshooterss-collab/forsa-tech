# خطة تنفيذية تفصيلية شاملة لمشروع فرصة-تك

## 📋 ملخص المشروع الحالي

### البنية الحالية
- **الإطار**: React.js مع Vite
- **قاعدة البيانات**: InsForge (سحابية)
- **التوجيه**: React Router
- **التصميم**: CSS مخصص مع نظام ثيمات
- **المكونات**: مكونات قابلة لإعادة الاستخدام

### الصفحات الحالية
1. الصفحة الرئيسية (HomePage) - متكاملة مع InsForge
2. صفحة البحث (SearchPage) - تعتمد على data.js
3. صفحة تفاصيل الإعلان (AdDetailPage) - متكاملة مع InsForge
4. صفحة نشر الإعلان (AdPostPage) - متكاملة مع InsForge
5. صفحة الفئات (CategoriesPage) - تعتمد على data.js
6. صفحة الدردشة (ChatPage) - تعتمد على data.js
7. صفحة الملف الشخصي (ProfilePage) - متكاملة جزئياً مع InsForge
8. صفحة المحفظة (WalletPage) - تعتمد على data.js
9. صفحة تسجيل الدخول (LoginPage) - متكاملة مع InsForge
10. صفحة التحقق (VerificationPage) - تحتاج مراجعة

### المكونات الحالية
1. Layout (التخطيط الرئيسي) - متكامل
2. AdCard (بطاقة الإعلان) - يحتاج تحسين
3. BackButton (زر العودة) - بسيط وجيد
4. ThemeContext (سياق الثيم) - بسيط وجيد
5. AuthContext (سياق المصادقة) - يحتاج تحسين

### البيانات الحالية
- **data.js**: ملف ضخم (41.2KB) يحتوي على:
  - الفئات الرئيسية (12 فئة)
  - الفئات الفرعية (عدد كبير)
  - الماركات (سيارات، موبايلات، إلكترونيات)
  - النماذج (لكل ماركة)
  - الإعلانات الوهمية
  - المحادثات الوهمية
  - المعاملات المالية الوهمية

### قاعدة البيانات InsForge
- **الجداول الحالية**:
  - `ads` - الإعلانات
  - `categories` - الفئات
  - `subcategories` - الفئات الفرعية
  - `profiles` - ملفات المستخدمين
  - `ad-images` - تخزين صور الإعلانات

---

## 🔴 المشاكل الحالية

### 1. مشاكل الأمان
- مفتاح API معروض بشكل صريح في `insforge.js`
- عدم استخدام متغيرات البيئة بشكل كامل
- عدم وجود التحقق من صحة البيانات
- عدم وجود معالجة أخطاء موحدة
- عدم وجود حماية من CSRF
- عدم وجود Rate Limiting

### 2. مشاكل الأداء
- ملف `data.js` ضخم (41.2KB)
- عدم وجود تخزين مؤقت للبيانات
- تحميل جميع البيانات دفعة واحدة
- عدم وجود تحميل كسول (lazy loading)
- عدم وجود Pagination
- عدم وجود Virtual Scrolling
- عدم وجود تحميل مسبق للصفحات
- عدم وجود تحسين للصور

### 3. مشاكل البنية
- عدم وجود فصل واضح بين العرض والمنطق
- مكونات كبيرة ومعقدة
- عدم وجود إدارة حالة مركزية
- عدم وجود معالجة أخطاء موحدة
- عدم وجود فصل واضح للمسارات
- عدم وجود تنظيم واضح للمكونات

### 4. مشاكل تجربة المستخدم
- عدم وجود تحميل مسبق للصفحات
- عدم وجود رسائل خطأ واضحة
- عدم وجود توجيه واضح للمستخدم
- عدم وجود إشعارات فورية
- عدم وجود تحميل مسبق للبيانات
- عدم وجود حفظ تلقائي للبيانات

### 5. مشاكل قاعدة البيانات
- بعض الصفحات تعتمد على بيانات وهمية
- عدم وجود علاقات واضحة بين الجداول
- عدم وجود فهارس محسنة
- عدم وجود سياسات أمان واضحة
- عدم وجود ترحيل للبيانات

### 6. مشاكل التكامل مع InsForge
- صفحة البحث تعتمد على data.js بدلاً من InsForge
- صفحة الدردشة تعتمد على data.js بدلاً من InsForge
- صفحة المحفظة تعتمد على data.js بدلاً من InsForge
- عدم وجود Real-time updates
- عدم وجود Subscription للبيانات

---

## 🟢 الحلول المقترحة - خطة تنفيذية تفصيلية

### المرحلة 1: الأساسيات (الأسبوع 1-2)

#### 1.1 تحسين الأمان

**المهمة 1.1.1: إنشاء ملف .env**
- إنشاء ملف `.env` في جذر المشروع:
```
VITE_INSFORGE_BASE_URL=https://63y9pesi.eu-central.insforge.app
VITE_INSFORGE_ANON_KEY=your_actual_key_here
VITE_APP_NAME=Forsa-Tech
VITE_APP_URL=http://localhost:5173
```

- إنشاء ملف `.env.example`:
```
VITE_INSFORGE_BASE_URL=your_insforge_base_url
VITE_INSFORGE_ANON_KEY=your_insforge_anon_key
VITE_APP_NAME=Forsa-Tech
VITE_APP_URL=http://localhost:5173
```

**المهمة 1.1.2: تحديث insforge.js**
```javascript
import { createClient } from '@insforge/sdk';

export const insforge = createClient({
    baseUrl: import.meta.env.VITE_INSFORGE_BASE_URL,
    anonKey: import.meta.env.VITE_INSFORGE_ANON_KEY
});
```

**المهمة 1.1.3: إنشاء نظام التحقق من صحة البيانات**
- إنشاء `src/utils/validation.js`:
```javascript
// التحقق من صحة الإعلان
export const validateAd = (ad) => {
    const errors = {}
    if (!ad.title || ad.title.length < 10) {
        errors.title = 'العنوان يجب أن يكون 10 أحرف على الأقل'
    }
    if (!ad.price || ad.price <= 0) {
        errors.price = 'السعر يجب أن يكون أكبر من صفر'
    }
    if (!ad.description || ad.description.length < 20) {
        errors.description = 'الوصف يجب أن يكون 20 حرف على الأقل'
    }
    if (!ad.category_id) {
        errors.category_id = 'يجب اختيار الفئة'
    }
    return errors
}

// التحقق من صحة المستخدم
export const validateUser = (user) => {
    const errors = {}
    if (!user.email || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(user.email)) {
        errors.email = 'البريد الإلكتروني غير صحيح'
    }
    if (!user.password || user.password.length < 6) {
        errors.password = 'كلمة المرور يجب أن تكون 6 أحرف على الأقل'
    }
    return errors
}
```

**المهمة 1.1.4: إنشاء نظام معالجة الأخطاء**
- إنشاء `src/utils/errorHandler.js`:
```javascript
export const handleApiError = (error) => {
    console.error('API Error:', error)

    if (error.message.includes('JWT')) {
        return 'جلسة منتهية، يرجى تسجيل الدخول مرة أخرى'
    }

    if (error.message.includes('Network')) {
        return 'مشكلة في الاتصال، يرجى المحاولة لاحقاً'
    }

    return error.message || 'حدث خطأ غير متوقع'
}

export const handleFormError = (errors) => {
    const firstError = Object.values(errors)[0]
    return firstError || 'يرجى تصحيح البيانات والمحاولة مرة أخرى'
}
```

#### 1.2 تحسين الأداء

**المهمة 1.2.1: تقسيم ملف data.js**
- إنشاء `src/data/categories.js`:
```javascript
export const categories = [
    // نسخ الفئات من data.js
]
```

- إنشاء `src/data/brands.js`:
```javascript
export const brands = {
    motors: [
        // نسخ ماركات السيارات من data.js
    ],
    mobiles: [
        // نسخ ماركات الموبايلات من data.js
    ],
    // ... باقي الماركات
}
```

- إنشاء `src/data/locations.js`:
```javascript
export const locations = [
    { id: 'cairo', name: 'القاهرة', districts: ['مدينة نصر', 'التجمع الخامس', 'المعادي'] },
    { id: 'giza', name: 'الجيزة', districts: ['الدقي', 'الهرم', 'الشيخ زايد'] },
    // ... باقي المدن
]
```

**المهمة 1.2.2: إضافة تحميل كسول**
- تحديث `src/App.jsx`:
```javascript
import { lazy, Suspense } from 'react'

const HomePage = lazy(() => import('./pages/HomePage'))
const SearchPage = lazy(() => import('./pages/SearchPage'))
const AdDetailPage = lazy(() => import('./pages/AdDetailPage'))
const AdPostPage = lazy(() => import('./pages/AdPostPage'))
const CategoriesPage = lazy(() => import('./pages/CategoriesPage'))
const ChatPage = lazy(() => import('./pages/ChatPage'))
const ProfilePage = lazy(() => import('./pages/ProfilePage'))
const WalletPage = lazy(() => import('./pages/WalletPage'))
const LoginPage = lazy(() => import('./pages/LoginPage'))
const VerificationPage = lazy(() => import('./pages/VerificationPage'))

// في Routes:
<Suspense fallback={<div>جاري التحميل...</div>}>
    <Route path="/" element={<Layout />}>
        <Route index element={<HomePage />} />
        <Route path="categories" element={<CategoriesPage />} />
        <Route path="search" element={<SearchPage />} />
        <Route path="ad/:id" element={<AdDetailPage />} />
        <Route path="post" element={requireAuth(<AdPostPage />)} />
        <Route path="chat" element={requireAuth(<ChatPage />)} />
        <Route path="profile" element={requireAuth(<ProfilePage />)} />
        <Route path="wallet" element={requireAuth(<WalletPage />)} />
        <Route path="verify" element={requireAuth(<VerificationPage />)} />
    </Route>
</Suspense>
```

**المهمة 1.2.3: إضافة تخزين مؤقت**
- إنشاء `src/utils/cache.js`:
```javascript
const cache = new Map()

export const withCache = (key, fetcher, ttl = 5 * 60 * 1000) => {
    const cached = cache.get(key)
    if (cached && Date.now() - cached.timestamp < ttl) {
        return Promise.resolve(cached.data)
    }

    return fetcher().then(data => {
        cache.set(key, {
            data,
            timestamp: Date.now()
        })
        return data
    })
}

export const clearCache = (pattern) => {
    if (!pattern) {
        cache.clear()
        return
    }

    for (const key of cache.keys()) {
        if (key.includes(pattern)) {
            cache.delete(key)
        }
    }
}
```

**المهمة 1.2.4: إضافة Pagination**
- إنشاء `src/hooks/usePagination.js`:
```javascript
import { useState, useEffect } from 'react'

export const usePagination = (fetchFunction, itemsPerPage = 10) => {
    const [items, setItems] = useState([])
    const [page, setPage] = useState(1)
    const [loading, setLoading] = useState(false)
    const [hasMore, setHasMore] = useState(true)

    const loadMore = async () => {
        if (loading || !hasMore) return
        setLoading(true)

        try {
            const { data } = await fetchFunction(page, itemsPerPage)
            setItems([...items, ...data])
            setHasMore(data.length === itemsPerPage)
            setPage(page + 1)
        } catch (error) {
            console.error('Error loading more items:', error)
        } finally {
            setLoading(false)
        }
    }

    const reset = () => {
        setItems([])
        setPage(1)
        setHasMore(true)
    }

    return { items, loadMore, loading, hasMore, reset }
}
```

#### 1.3 تحسين البنية

**المهمة 1.3.1: تنظيم المجلدات**
- إنشاء الهيكل التالي:
```
src/
├── components/
│   ├── common/
│   │   ├── Button.jsx
│   │   ├── Input.jsx
│   │   ├── Card.jsx
│   │   ├── Modal.jsx
│   │   └── Loading.jsx
│   ├── ads/
│   │   ├── AdCard.jsx
│   │   ├── AdGrid.jsx
│   │   └── AdFilters.jsx
│   ├── forms/
│   │   ├── AdForm.jsx
│   │   └── SearchForm.jsx
│   └── layout/
│       ├── Layout.jsx
│       ├── Header.jsx
│       ├── Sidebar.jsx
│       └── BottomNav.jsx
├── pages/
│   ├── auth/
│   │   ├── LoginPage.jsx
│   │   └── VerificationPage.jsx
│   ├── ads/
│   │   ├── AdDetailPage.jsx
│   │   ├── AdPostPage.jsx
│   │   └── SearchPage.jsx
│   ├── categories/
│   │   └── CategoriesPage.jsx
│   └── user/
│       ├── ProfilePage.jsx
│       ├── ChatPage.jsx
│       └── WalletPage.jsx
├── hooks/
│   ├── useAuth.js
│   ├── useAds.js
│   ├── usePagination.js
│   └── useCache.js
├── utils/
│   ├── validation.js
│   ├── errorHandler.js
│   ├── cache.js
│   └── helpers.js
├── services/
│   ├── ads.js
│   ├── auth.js
│   ├── chat.js
│   └── wallet.js
├── context/
│   ├── AuthContext.jsx
│   ├── ThemeContext.jsx
│   └── NotificationContext.jsx
└── data/
    ├── categories.js
    ├── brands.js
    └── locations.js
```

**المهمة 1.3.2: إنشاء Hooks مخصصة**
- إنشاء `src/hooks/useAuth.js`:
```javascript
import { useContext } from 'react'
import { AuthContext } from '../context/AuthContext'

export const useAuth = () => {
    const context = useContext(AuthContext)
    if (!context) {
        throw new Error('useAuth must be used within AuthProvider')
    }
    return context
}
```

- إنشاء `src/hooks/useAds.js`:
```javascript
import { useState, useEffect } from 'react'
import { insforge } from '../lib/insforge'

export const useAds = (filters = {}) => {
    const [ads, setAds] = useState([])
    const [loading, setLoading] = useState(true)
    const [error, setError] = useState(null)

    useEffect(() => {
        const fetchAds = async () => {
            try {
                setLoading(true)
                let query = insforge.db.from('ads').select('*')

                Object.entries(filters).forEach(([key, value]) => {
                    if (value) {
                        query = query.eq(key, value)
                    }
                })

                const { data, error } = await query
                if (error) throw error
                setAds(data)
            } catch (err) {
                setError(err)
            } finally {
                setLoading(false)
            }
        }
        fetchAds()
    }, [filters])

    return { ads, loading, error }
}
```

**المهمة 1.3.3: إنشاء Services**
- إنشاء `src/services/ads.js`:
```javascript
import { insforge } from '../lib/insforge'

export const adsService = {
    getAll: async (filters = {}) => {
        let query = insforge.db.from('ads').select('*')

        Object.entries(filters).forEach(([key, value]) => {
            if (value) {
                query = query.eq(key, value)
            }
        })

        const { data, error } = await query
        if (error) throw error
        return data
    },

    getById: async (id) => {
        const { data, error } = await insforge.db
            .from('ads')
            .select('*')
            .eq('id', id)
            .single()
        if (error) throw error
        return data
    },

    create: async (ad) => {
        const { data, error } = await insforge.db
            .from('ads')
            .insert([ad])
            .select()
            .single()
        if (error) throw error
        return data
    },

    update: async (id, updates) => {
        const { data, error } = await insforge.db
            .from('ads')
            .update(updates)
            .eq('id', id)
            .select()
            .single()
        if (error) throw error
        return data
    },

    delete: async (id) => {
        const { error } = await insforge.db
            .from('ads')
            .delete()
            .eq('id', id)
        if (error) throw error
    },

    getSimilar: async (categoryId, excludeId) => {
        const { data, error } = await insforge.db
            .from('ads')
            .select('*')
            .eq('category_id', categoryId)
            .neq('id', excludeId)
            .limit(6)
        if (error) throw error
        return data
    }
}
```

### المرحلة 2: التكامل مع InsForge (الأسبوع 3-4)

#### 2.1 تحديث صفحة البحث

**المهمة 2.1.1: تحديث SearchPage لاستخدام InsForge**
- استبدال البيانات الوهمية ببيانات من InsForge
- استخدام `useAds` hook
- إضافة فلاتر حقيقية
- إضافة Pagination

```javascript
// src/pages/ads/SearchPage.jsx
import { useState, useMemo } from 'react'
import { useNavigate, useSearchParams } from 'react-router-dom'
import { useAds } from '../../hooks/useAds'
import { categories } from '../../data/categories'

export default function SearchPage() {
    const nav = useNavigate()
    const [params, setSearchParams] = useSearchParams()

    const catName = params.get('cat') || 'الكل'
    const subName = params.get('sub') || 'الكل'
    const q = params.get('q') || ''

    const [type, setType] = useState('rent')

    const activeCategory = useMemo(() => 
        categories.find(c => c.name === catName), 
        [catName]
    )

    const filters = useMemo(() => {
        const f = {}
        if (catName !== 'الكل') f.category_name = catName
        if (subName !== 'الكل') f.subcategory_name = subName
        if (q) f.title = q
        return f
    }, [catName, subName, q])

    const { ads, loading, error } = useAds(filters)

    // ... باقي الكود
}
```

#### 2.2 تحديث صفحة الدردشة

**المهمة 2.2.1: إنشاء جداول الدردشة في InsForge**
```sql
-- جدول المحادثات
CREATE TABLE conversations (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user1_id UUID REFERENCES auth.users(id),
    user2_id UUID REFERENCES auth.users(id),
    ad_id UUID REFERENCES ads(id),
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    UNIQUE(user1_id, user2_id, ad_id)
);

-- جدول الرسائل
CREATE TABLE messages (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    conversation_id UUID REFERENCES conversations(id),
    sender_id UUID REFERENCES auth.users(id),
    content TEXT NOT NULL,
    read_at TIMESTAMP WITH TIME ZONE,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- إنشاء فهارس
CREATE INDEX idx_conversations_user1 ON conversations(user1_id);
CREATE INDEX idx_conversations_user2 ON conversations(user2_id);
CREATE INDEX idx_messages_conversation ON messages(conversation_id);
CREATE INDEX idx_messages_created ON messages(created_at);
```

**المهمة 2.2.2: إنشاء خدمة الدردشة**
```javascript
// src/services/chat.js
import { insforge } from '../lib/insforge'

export const chatService = {
    getConversations: async (userId) => {
        const { data, error } = await insforge.db
            .from('conversations')
            .select(`
                *,
                user1:profiles!conversations_user1_id_fkey(*),
                user2:profiles!conversations_user2_id_fkey(*),
                ad:ads(*),
                messages(count)
            `)
            .or(`user1_id.eq.${userId},user2_id.eq.${userId}`)
            .order('updated_at', { ascending: false })

        if (error) throw error
        return data
    },

    getMessages: async (conversationId) => {
        const { data, error } = await insforge.db
            .from('messages')
            .select('*')
            .eq('conversation_id', conversationId)
            .order('created_at', { ascending: true })

        if (error) throw error
        return data
    },

    sendMessage: async (conversationId, senderId, content) => {
        const { data, error } = await insforge.db
            .from('messages')
            .insert([{
                conversation_id: conversationId,
                sender_id: senderId,
                content
            }])
            .select()
            .single()

        if (error) throw error

        // تحديث وقت آخر رسالة
        await insforge.db
            .from('conversations')
            .update({ updated_at: new Date() })
            .eq('id', conversationId)

        return data
    },

    markAsRead: async (conversationId, userId) => {
        const { error } = await insforge.db
            .from('messages')
            .update({ read_at: new Date() })
            .eq('conversation_id', conversationId)
            .neq('sender_id', userId)
            .is('read_at', null)

        if (error) throw error
    },

    subscribeToMessages: (conversationId, callback) => {
        return insforge
            .channel(`messages:${conversationId}`)
            .on('postgres_changes', {
                event: 'INSERT',
                schema: 'public',
                table: 'messages',
                filter: `conversation_id=eq.${conversationId}`
            }, callback)
            .subscribe()
    }
}
```

**المهمة 2.2.3: تحديث ChatPage**
```javascript
// src/pages/user/ChatPage.jsx
import { useState, useEffect } from 'react'
import { useAuth } from '../../hooks/useAuth'
import { chatService } from '../../services/chat'

export default function ChatPage() {
    const { user } = useAuth()
    const [conversations, setConversations] = useState([])
    const [activeChat, setActiveChat] = useState(null)
    const [messages, setMessages] = useState([])
    const [loading, setLoading] = useState(true)

    useEffect(() => {
        const fetchConversations = async () => {
            try {
                const data = await chatService.getConversations(user.id)
                setConversations(data)
            } catch (error) {
                console.error('Error fetching conversations:', error)
            } finally {
                setLoading(false)
            }
        }
        fetchConversations()
    }, [user])

    useEffect(() => {
        if (!activeChat) return

        const fetchMessages = async () => {
            const data = await chatService.getMessages(activeChat.id)
            setMessages(data)

            // اشتراك في الرسائل الجديدة
            const subscription = chatService.subscribeToMessages(
                activeChat.id,
                (payload) => {
                    setMessages([...messages, payload.new])
                }
            )

            return () => subscription.unsubscribe()
        }
        fetchMessages()
    }, [activeChat])

    const sendMessage = async (content) => {
        await chatService.sendMessage(activeChat.id, user.id, content)
    }

    // ... باقي الكود
}
```

#### 2.3 تحديث صفحة المحفظة

**المهمة 2.3.1: إنشاء جداول المحفظة في InsForge**
```sql
-- جدول المعاملات
CREATE TABLE transactions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES auth.users(id),
    type VARCHAR(20) NOT NULL, -- 'credit', 'debit', 'transfer'
    amount DECIMAL(10, 2) NOT NULL,
    description TEXT,
    reference_id UUID,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- جدول رصيد المستخدم
CREATE TABLE user_balance (
    user_id UUID PRIMARY KEY REFERENCES auth.users(id),
    balance DECIMAL(10, 2) DEFAULT 0,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- إنشاء فهارس
CREATE INDEX idx_transactions_user ON transactions(user_id);
CREATE INDEX idx_transactions_created ON transactions(created_at);
```

**المهمة 2.3.2: إنشاء خدمة المحفظة**
```javascript
// src/services/wallet.js
import { insforge } from '../lib/insforge'

export const walletService = {
    getBalance: async (userId) => {
        const { data, error } = await insforge.db
            .from('user_balance')
            .select('*')
            .eq('user_id', userId)
            .single()

        if (error) throw error
        return data?.balance || 0
    },

    getTransactions: async (userId, limit = 20) => {
        const { data, error } = await insforge.db
            .from('transactions')
            .select('*')
            .eq('user_id', userId)
            .order('created_at', { ascending: false })
            .limit(limit)

        if (error) throw error
        return data
    },

    addFunds: async (userId, amount, description) => {
        // 1. إضافة المعاملة
        const { data: tx, error: txError } = await insforge.db
            .from('transactions')
            .insert([{
                user_id: userId,
                type: 'credit',
                amount,
                description
            }])
            .select()
            .single()

        if (txError) throw txError

        // 2. تحديث الرصيد
        const { error: balanceError } = await insforge.rpc('update_balance', {
            p_user_id: userId,
            p_amount: amount
        })

        if (balanceError) throw balanceError

        return tx
    },

    transferFunds: async (fromUserId, toUserId, amount, description) => {
        // 1. خصم من المرسل
        await walletService.deductFunds(fromUserId, amount, `تحويل إلى ${toUserId}`)

        // 2. إضافة للمستلم
        await walletService.addFunds(toUserId, amount, `تحويل من ${fromUserId}`)

        return true
    },

    deductFunds: async (userId, amount, description) => {
        // 1. إضافة المعاملة
        const { data: tx, error: txError } = await insforge.db
            .from('transactions')
            .insert([{
                user_id: userId,
                type: 'debit',
                amount: -amount,
                description
            }])
            .select()
            .single()

        if (txError) throw txError

        // 2. تحديث الرصيد
        const { error: balanceError } = await insforge.rpc('update_balance', {
            p_user_id: userId,
            p_amount: -amount
        })

        if (balanceError) throw balanceError

        return tx
    }
}
```

**المهمة 2.3.3: تحديث WalletPage**
```javascript
// src/pages/user/WalletPage.jsx
import { useState, useEffect } from 'react'
import { useAuth } from '../../hooks/useAuth'
import { walletService } from '../../services/wallet'

export default function WalletPage() {
    const { user } = useAuth()
    const [balance, setBalance] = useState(0)
    const [transactions, setTransactions] = useState([])
    const [loading, setLoading] = useState(true)

    useEffect(() => {
        const fetchData = async () => {
            try {
                const [bal, txs] = await Promise.all([
                    walletService.getBalance(user.id),
                    walletService.getTransactions(user.id)
                ])
                setBalance(bal)
                setTransactions(txs)
            } catch (error) {
                console.error('Error fetching wallet data:', error)
            } finally {
                setLoading(false)
            }
        }
        fetchData()
    }, [user])

    // ... باقي الكود
}
```

### المرحلة 3: الميزات المتقدمة (الأسبوع 5-6)

#### 3.1 نظام البحث المتقدم

**المهمة 3.1.1: إنشاء مكون البحث المتقدم**
```javascript
// src/components/ads/AdvancedSearch.jsx
import { useState } from 'react'
import { categories } from '../../data/categories'

export default function AdvancedSearch({ onSearch }) {
    const [filters, setFilters] = useState({
        category: '',
        subcategory: '',
        priceRange: [0, 1000000],
        location: '',
        condition: '',
        sortBy: 'newest'
    })

    const handleSearch = () => {
        onSearch(filters)
    }

    return (
        <div className="advanced-search">
            {/* فلاتر متقدمة */}
        </div>
    )
}
```

**المهمة 3.1.2: إضافة حفظ عمليات البحث**
```javascript
// src/hooks/useSavedSearches.js
import { useState, useEffect } from 'react'
import { insforge } from '../lib/insforge'

export const useSavedSearches = (userId) => {
    const [searches, setSearches] = useState([])

    useEffect(() => {
        const fetchSearches = async () => {
            const { data } = await insforge.db
                .from('saved_searches')
                .select('*')
                .eq('user_id', userId)
                .order('created_at', { ascending: false })

            setSearches(data || [])
        }
        fetchSearches()
    }, [userId])

    const saveSearch = async (search) => {
        const { data, error } = await insforge.db
            .from('saved_searches')
            .insert([{
                user_id: userId,
                filters: JSON.stringify(search.filters),
                name: search.name
            }])
            .select()
            .single()

        if (error) throw error
        setSearches([...searches, data])
        return data
    }

    const deleteSearch = async (searchId) => {
        const { error } = await insforge.db
            .from('saved_searches')
            .delete()
            .eq('id', searchId)

        if (error) throw error
        setSearches(searches.filter(s => s.id !== searchId))
    }

    return { searches, saveSearch, deleteSearch }
}
```

#### 3.2 نظام الإشعارات

**المهمة 3.2.1: إنشاء جدول الإشعارات**
```sql
CREATE TABLE notifications (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES auth.users(id),
    type VARCHAR(50) NOT NULL,
    title TEXT NOT NULL,
    message TEXT,
    data JSONB,
    read_at TIMESTAMP WITH TIME ZONE,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE INDEX idx_notifications_user ON notifications(user_id);
CREATE INDEX idx_notifications_read ON notifications(user_id, read_at);
```

**المهمة 3.2.2: إنشاء خدمة الإشعارات**
```javascript
// src/services/notifications.js
import { insforge } from '../lib/insforge'

export const notificationService = {
    getNotifications: async (userId, limit = 20) => {
        const { data, error } = await insforge.db
            .from('notifications')
            .select('*')
            .eq('user_id', userId)
            .order('created_at', { ascending: false })
            .limit(limit)

        if (error) throw error
        return data
    },

    markAsRead: async (notificationId) => {
        const { error } = await insforge.db
            .from('notifications')
            .update({ read_at: new Date() })
            .eq('id', notificationId)

        if (error) throw error
    },

    markAllAsRead: async (userId) => {
        const { error } = await insforge.db
            .from('notifications')
            .update({ read_at: new Date() })
            .eq('user_id', userId)
            .is('read_at', null)

        if (error) throw error
    },

    subscribeToNotifications: (userId, callback) => {
        return insforge
            .channel(`notifications:${userId}`)
            .on('postgres_changes', {
                event: 'INSERT',
                schema: 'public',
                table: 'notifications',
                filter: `user_id=eq.${userId}`
            }, callback)
            .subscribe()
    }
}
```

**المهمة 3.2.3: إنشاء NotificationContext**
```javascript
// src/context/NotificationContext.jsx
import { createContext, useContext, useState, useEffect } from 'react'
import { useAuth } from '../hooks/useAuth'
import { notificationService } from '../services/notifications'

export const NotificationContext = createContext(null)

export const NotificationProvider = ({ children }) => {
    const { user } = useAuth()
    const [notifications, setNotifications] = useState([])
    const [unreadCount, setUnreadCount] = useState(0)

    useEffect(() => {
        if (!user) return

        const fetchNotifications = async () => {
            const data = await notificationService.getNotifications(user.id)
            setNotifications(data)
            setUnreadCount(data.filter(n => !n.read_at).length)
        }
        fetchNotifications()

        // اشتراك في الإشعارات الجديدة
        const subscription = notificationService.subscribeToNotifications(
            user.id,
            (payload) => {
                setNotifications([payload.new, ...notifications])
                setUnreadCount(unreadCount + 1)
            }
        )

        return () => subscription.unsubscribe()
    }, [user])

    const markAsRead = async (notificationId) => {
        await notificationService.markAsRead(notificationId)
        setNotifications(notifications.map(n => 
            n.id === notificationId ? { ...n, read_at: new Date() } : n
        ))
        setUnreadCount(Math.max(0, unreadCount - 1))
    }

    return (
        <NotificationContext.Provider value={{
            notifications,
            unreadCount,
            markAsRead
        }}>
            {children}
        </NotificationContext.Provider>
    )
}

export const useNotifications = () => {
    const context = useContext(NotificationContext)
    if (!context) {
        throw new Error('useNotifications must be used within NotificationProvider')
    }
    return context
}
```

### المرحلة 4: التحسينات النهائية (الأسبوع 7-8)

#### 4.1 تحسينات الأداء

**المهمة 4.1.1: تحسين الصور**
- إضافة تحميل كسول للصور
- تحسين حجم الصور
- إضافة WebP format
- إضافة placeholder images

```javascript
// src/components/common/OptimizedImage.jsx
import { useState } from 'react'

export default function OptimizedImage({ src, alt, ...props }) {
    const [loaded, setLoaded] = useState(false)

    return (
        <div className="optimized-image" {...props}>
            {!loaded && <div className="image-placeholder" />}
            <img
                src={src}
                alt={alt}
                loading="lazy"
                onLoad={() => setLoaded(true)}
                style={{ opacity: loaded ? 1 : 0 }}
            />
        </div>
    )
}
```

**المهمة 4.1.2: إضافة Virtual Scrolling**
```javascript
// src/components/common/VirtualList.jsx
import { useState, useRef, useEffect } from 'react'

export default function VirtualList({ items, renderItem, itemHeight }) {
    const [scrollTop, setScrollTop] = useState(0)
    const containerRef = useRef(null)
    const containerHeight = 600

    const visibleCount = Math.ceil(containerHeight / itemHeight)
    const startIndex = Math.floor(scrollTop / itemHeight)
    const endIndex = Math.min(startIndex + visibleCount, items.length)

    const visibleItems = items.slice(startIndex, endIndex)

    return (
        <div 
            ref={containerRef}
            className="virtual-list"
            style={{ height: containerHeight, overflow: 'auto' }}
            onScroll={e => setScrollTop(e.target.scrollTop)}
        >
            <div style={{ height: items.length * itemHeight }}>
                {visibleItems.map((item, i) => (
                    <div 
                        key={startIndex + i}
                        style={{
                            position: 'absolute',
                            top: (startIndex + i) * itemHeight,
                            height: itemHeight
                        }}
                    >
                        {renderItem(item)}
                    </div>
                ))}
            </div>
        </div>
    )
}
```

#### 4.2 تحسينات تجربة المستخدم

**المهمة 4.2.1: إضافة Toast Notifications**
```javascript
// src/components/common/Toast.jsx
import { useState, useEffect } from 'react'

export default function Toast({ message, type = 'success', duration = 3000, onClose }) {
    const [visible, setVisible] = useState(true)

    useEffect(() => {
        const timer = setTimeout(() => {
            setVisible(false)
            setTimeout(onClose, 300)
        }, duration)

        return () => clearTimeout(timer)
    }, [duration, onClose])

    return (
        <div className={`toast toast-${type} ${visible ? 'visible' : ''}`}>
            <span className="toast-icon">
                {type === 'success' && '✅'}
                {type === 'error' && '❌'}
                {type === 'warning' && '⚠️'}
                {type === 'info' && 'ℹ️'}
            </span>
            <span className="toast-message">{message}</span>
        </div>
    )
}
```

**المهمة 4.2.2: إضافة Loading States**
```javascript
// src/components/common/Loading.jsx
export default function Loading({ size = 'medium', text = 'جاري التحميل...' }) {
    const sizes = {
        small: 20,
        medium: 40,
        large: 60
    }

    return (
        <div className="loading-container">
            <div 
                className="spinner"
                style={{ width: sizes[size], height: sizes[size] }}
            />
            {text && <p className="loading-text">{text}</p>}
        </div>
    )
}
```

#### 4.3 تحسينات الأمان

**المهمة 4.3.1: إضافة Rate Limiting**
```javascript
// src/utils/rateLimiter.js
const requests = new Map()

export const rateLimiter = (key, limit = 5, window = 60000) => {
    const now = Date.now()
    const userRequests = requests.get(key) || []

    // تنظيف الطلبات القديمة
    const recentRequests = userRequests.filter(time => now - time < window)

    if (recentRequests.length >= limit) {
        return false
    }

    recentRequests.push(now)
    requests.set(key, recentRequests)
    return true
}
```

**المهمة 4.3.2: إضافة CSRF Protection**
```javascript
// src/utils/csrf.js
export const getCSRFToken = () => {
    return localStorage.getItem('csrf_token')
}

export const setCSRFToken = (token) => {
    localStorage.setItem('csrf_token', token)
}

export const generateCSRFToken = () => {
    const token = Array.from(crypto.getRandomValues(new Uint8Array(32)))
        .map(b => b.toString(16).padStart(2, '0'))
        .join('')
    setCSRFToken(token)
    return token
}
```

---

## 📊 خارطة الطريق التفصيلية

### الأسبوع 1: الأساسيات والأمان
- [ ] إنشاء ملف .env
- [ ] تحديث insforge.js
- [ ] إنشاء نظام التحقق من صحة البيانات
- [ ] إنشاء نظام معالجة الأخطاء
- [ ] تنظيم المجلدات
- [ ] إنشاء Hooks مخصصة

### الأسبوع 2: تحسين الأداء
- [ ] تقسيم ملف data.js
- [ ] إضافة تحميل كسول
- [ ] إضافة تخزين مؤقت
- [ ] إضافة Pagination
- [ ] تحسين تحميل الصور

### الأسبوع 3: التكامل مع InsForge - الجزء 1
- [ ] تحديث صفحة البحث
- [ ] إنشاء جداول الدردشة
- [ ] إنشاء خدمة الدردشة
- [ ] تحديث ChatPage

### الأسبوع 4: التكامل مع InsForge - الجزء 2
- [ ] إنشاء جداول المحفظة
- [ ] إنشاء خدمة المحفظة
- [ ] تحديث WalletPage
- [ ] إضافة Real-time updates

### الأسبوع 5: الميزات المتقدمة - الجزء 1
- [ ] إنشاء مكون البحث المتقدم
- [ ] إضافة حفظ عمليات البحث
- [ ] تحسين فلاتر البحث
- [ ] إضافة اقتراحات البحث

### الأسبوع 6: الميزات المتقدمة - الجزء 2
- [ ] إنشاء جدول الإشعارات
- [ ] إنشاء خدمة الإشعارات
- [ ] إنشاء NotificationContext
- [ ] إضافة إشعارات فورية

### الأسبوع 7: التحسينات النهائية - الجزء 1
- [ ] تحسين الصور
- [ ] إضافة Virtual Scrolling
- [ ] تحسين الأداء العام
- [ ] تحسين SEO

### الأسبوع 8: التحسينات النهائية - الجزء 2
- [ ] إضافة Toast Notifications
- [ ] إضافة Loading States
- [ ] إضافة Rate Limiting
- [ ] إضافة CSRF Protection

---

## 🎯 النتائج المتوقعة

### تحسينات الأداء
- تقليل وقت التحميل بنسبة 50%
- تقليل حجم التطبيق بنسبة 30%
- تحسين استجابة التطبيق
- تحسين تجربة المستخدم على الأجهزة البطيئة

### تحسينات تجربة المستخدم
- زيادة معدل الاحتفاظ بالمستخدمين بنسبة 40%
- تقليل معدل الارتداد بنسبة 30%
- زيادة معدل التحويل بنسبة 25%
- تحسين رضا المستخدمين

### تحسينات الأمان
- حماية كاملة من الثغرات الشائعة
- التحقق من صحة جميع البيانات
- سياسات أمان قوية
- حماية من الهجمات الشائعة

---

## 📝 ملاحظات مهمة للتنفيذ

### التنفيذ المتدرج
1. ابدأ بالمشاكل الأكثر إلحاحاً
2. ركز على تحسينات ذات تأثير كبير
3. اختبر كل تغيير قبل الانتقال للتالي
4. اجمع الملاحظات من المستخدمين باستمرار

### المعايير
- قياس الأداء قبل وبعد كل تغيير
- تتبع معدلات التحويل
- مراقبة الأخطاء والأداء
- جمع ملاحظات المستخدمين

### التوثيق
- وثق كل التغييرات
- احتفظ بسجل التغييرات
- قم بتحديث التوثيق بانتظام
- شارك المعرفة مع الفريق

---

هذه الخطة التنفيذية التفصيلية ستساعدك على تحويل مشروع فرصة-تك إلى منصة احترافية تتفوق على المنافسين. التركيز على التنفيذ المتدرج والتحسين المستمر هو المفتاح للنجاح.
